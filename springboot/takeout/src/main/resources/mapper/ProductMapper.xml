<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.takeout.mapper.ProductMapper">

    <select id="selectList" resultType="com.cpy.takeout.entity.Product">
        select * from product order by create_time desc limit #{start},#{size}
    </select>

    <select id="selectById" resultType="com.cpy.takeout.entity.Product">
        select * from product where id=#{id}
    </select>

    <insert id="insert">
        insert into product(name,price,description,image,status)
        values(#{name},#{price},#{description},#{image},#{status})
    </insert>

    <update id="update">
        update product
        <set>
            <if test="name != null">name=#{name},</if>
            <if test="price != null">price=#{price},</if>
            <if test="description != null">description=#{description},</if>
            <if test="image != null">image=#{image},</if>
            <if test="status != null">status=#{status}</if>
        </set>
        where id=#{id}
    </update>

    <delete id="deleteById">
        delete from product where id=#{id}
    </delete>

</mapper>